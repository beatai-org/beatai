1. 每次跑 run.js 都调 medium-sub 拿新候选（删 "复用当日 sub-list" 分支，--refresh-list flag 同时下线）
2. dedup 真相源 = 当天 + 过去 2 天共 3 个 sub-list 文件（与你列的 5/16、5/17、5/18 一致）
3. 新候选与 3 个 sub-list 求差集 → 本次新增 slug
4. 本次新增 merge 进当日 sub-list（不覆盖、不丢失今日已处理记录）—— 保证 sub-list 始终是"今日所有进过流水线的 slug"的累计集合
5. fetch + handoff 只处理本次新增；当日 sub-list 里已有的 slug 不再 fetch 也不再翻译
6. 效果：今天再跑、medium 没新文章 → 本次新增 = 0 → 不 fetch、不 handoff、不 publish